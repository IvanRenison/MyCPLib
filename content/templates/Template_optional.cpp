//

#include <bits/stdc++.h>

using namespace std;

typedef unsigned long long ull;
typedef long long ll;
#define fore(i, a, b) for (ull i = a, gmat = b; i < gmat; i++)


optional<void> solve(void) {

}

int main(void) {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL), cout.tie(NULL);


  auto ans = solve();

  if (ans.has_value()) {
    cout << *ans << '\n';
  } else {
    cout << "-1\n";
  }

  return EXIT_SUCCESS;
}
